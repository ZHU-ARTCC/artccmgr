New feedback.

<%= form_for :feedback  do |f| %>
    Submitter: <%= current_user.name_first %>
               <%= current_user.name_last %>
               (<%= current_user.cid %>)
    <br />
    Flight Callsign: <%= f.text_field :callsign %> <br/>
    <% controllers = Group.find_by(name: 'controller').users.collect {|a| [ a.name, a.name ]} %>
    <% controllers.unshift ['Unknown', 'Unknown'] %>
    Controller: <%= f.select :controller, controllers %>
<% end %>